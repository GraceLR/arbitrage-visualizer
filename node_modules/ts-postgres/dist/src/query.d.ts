/// <reference types="node" />
import { Writable } from 'stream';
import { DataFormat, DataType, Value } from './types';
export interface QueryOptions {
    readonly name: string;
    readonly portal: string;
    readonly types: DataType[];
    readonly format: DataFormat | DataFormat[];
    readonly streams: Record<string, Writable>;
}
export declare class Query {
    readonly text: string;
    readonly values?: Value[] | undefined;
    readonly options?: Partial<QueryOptions> | undefined;
    constructor(text: string, values?: Value[] | undefined, options?: Partial<QueryOptions> | undefined);
}
